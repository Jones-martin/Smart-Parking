name: email_info_sender
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  razorpay_flutter: ^1.3.7
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2
  geolocator: ^11.0.0      
  geocoding: ^3.0.0        
  intl: ^0.20.2
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
